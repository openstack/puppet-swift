---
upgrade:
  - |
    Support for amqp1 messaging driver by the ``swift::proxy::ceilometer``
    class has been removed.

  - |
    The ``swift::proxy::ceilometer::amqp_ssl_key_password`` parameter has
    been deprecated and has no effect now.
